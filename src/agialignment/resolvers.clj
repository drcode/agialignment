(ns agialignment.resolvers
  (:require [fbc-utils.core :as ut]
            [datahike.api :as dh]
            [com.walmartlabs.lacinia.schema :as sc]
            [super-server.super-server :as ss]))

(defn app [db context args value]
  {:id "app:0"})
